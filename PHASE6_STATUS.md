# Ah Studio Phase 6

Professional export engine foundation is staged on `phase-6-professional-export`.

The CI workflow integrates the Phase 6 export orchestration into `StudioViewModel` before running unit tests and the Android debug build. No AutoCut dependency is used.
